/*
const fs = require("fs").promises;
const path = require("path");
const sDir = app.getPath("appData");

fs.access(path.join(sDir, "database.sqlite3"), fs.constants.F_OK).then(() => {

}).catch(() => {
    "base_database.sqlite3"
}); */
